/**
 * ! "Better Comments" Extension 을 사용하고 있습니다.
 * ? .vscode 의 설정이 포함되어 있으니 가독성을 위해, extension을 사용해주세요.
 */

/**
 * ! Number types
 * objective : 김두현
 *
 */

// TODO: should be assigned boolean value
const var__boolean: void = undefined;

// TODO: should be assigned suitable number value
const var__decimal: void = undefined;
const var__hex: void = undefined;
const var__binary: void = undefined;
const var__octal: void = undefined;

// TODO: should be assigned suitable string value
const var__string: void = undefined;

/**
 * ! Array & Tuple
 * objective : rlrlrlrl
 * * Array types can be written in one of two ways in typescripts.
 *
 * const variable_name: number[] = [1,2,3];
 * ? using bracket
 *
 * const variable_name: Array<number> = [1,2,3];
 * ? using generic array type :
 *
 * * Tuple types allow me to express an array with a fixed number of elements whose types are known.
 *
 *
 *
 */
const var__list__bracket: void = undefined;
const var__list__generic: void = undefined;

export = {
  var__boolean,
  var__decimal,
  var__hex,
  var__binary,
  var__octal,
  var__string,
  var__list__bracket,
  var__list__generic
};
